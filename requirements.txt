# BEWARE of any additional dependencies described in the README!

# EOL of 3.10 is planned for 10-2026,
# leaving the required 3 years of time after the project starts.
python_requires='>=3.10'

# General requirements
argparse >= 1.1
logging >= 0.5.1.2
PyYAML >= 6.0

# Calculating
bitmath >= 1.3.3.1
pandas >= 1.5.3
matplotlib >= 3.7.1

# Communication and networking
socket
subprocess
# threading
# queue
# signal